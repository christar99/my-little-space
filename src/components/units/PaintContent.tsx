import React from 'react';
import { programType } from 'utils/type';

interface PaintContentProps {
	program: programType;
}

function PaintContent({ program }: PaintContentProps) {
	return <div>PaintContent</div>;
}

export default PaintContent;
